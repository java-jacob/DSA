package com.StackAndQueue;

/**
 * Size inside the queue keep track of the queue size.
 * But this can be overhead while inserting and deleting.
 * To avoid this size can be dynamically calculated
 * using front and rear.
 * P
 *
 */
public class IntQueueWithoutSize {
}
